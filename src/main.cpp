#include "CMenu.hpp"

/**
 * to start the game, all that's required is to create an instance of CMenu and call
 * the mainMenu() function
 */
int main() {
    CMenu m;
    m.mainMenu();
    return 0;
}